function loadService() {
    var page = document.getElementById("page");
    var template = `
        <h4>1. Book a one hour consultation</h4>
        <p>
            Based on your project details and design preferences, we will hand-select one of our top designers to meet with you for a one hour consult.
        </p>
    `;

    page.innerHTML = template;
}